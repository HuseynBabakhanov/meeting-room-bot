# 🚀 Деплой на DigitalOcean App Platform

## 📋 Что вам понадобится:

- ✅ Аккаунт на DigitalOcean (https://www.digitalocean.com/)
- ✅ GitHub репозиторий (уже готов: https://github.com/HuseynBabakhanov/meeting-room-bot)
- ✅ Токен бота (у вас есть: 8278603173:AAFQsjYB5Ms6P_7TSbEOyEdqYMs83nZ7Afs)

---

## 🎯 Пошаговая инструкция:

### Шаг 1: Создание PostgreSQL базы данных

1. Войдите на https://cloud.digitalocean.com/
2. Перейдите в **Databases** → **Create Database Cluster**
3. Выберите:
   - Engine: **PostgreSQL**
   - Version: **15** (или последняя)
   - Datacenter: **New York** или ближайший к вам
   - Plan: **Basic** ($15/месяц) или **Development** (бесплатно, если доступно)
4. Нажмите **Create Database Cluster**
5. Дождитесь создания (1-2 минуты)
6. **ВАЖНО:** Скопируйте **Connection String** (будет выглядеть так):
   ```
   postgresql://doadmin:пароль@db-postgresql-nyc-12345.ondigitalocean.com:25060/defaultdb?sslmode=require
   ```

### Шаг 2: Создание приложения (App)

1. Перейдите в **Apps** → **Create App**
2. Выберите **GitHub** как источник кода
3. Авторизуйте DigitalOcean в GitHub (если еще не сделали)
4. Выберите репозиторий: **HuseynBabakhanov/meeting-room-bot**
5. Ветка: **main**
6. Нажмите **Next**

### Шаг 3: Настройка приложения

1. **Resources:**
   - Type: **Worker** (не Web Service!)
   - Name: meeting-room-bot
   - Run Command: `python bot.py` (должно подставиться автоматически)

2. **Environment Variables (Переменные окружения):**
   Нажмите **Edit** и добавьте:

   | Key            | Value                                            | Encrypt |
   | -------------- | ------------------------------------------------ | ------- |
   | `BOT_TOKEN`    | `8278603173:AAFQsjYB5Ms6P_7TSbEOyEdqYMs83nZ7Afs` | ✅      |
   | `DATABASE_URL` | `postgresql://...` (ваша строка подключения)     | ✅      |
   | `GROUP_CHAT_ID` | `-100123456789` (ID вашей группы, опционально)  | ❌      |

   **ВАЖНО:**
   - Для `DATABASE_URL` вставьте полную строку подключения из вашей базы данных DigitalOcean
   - `GROUP_CHAT_ID` - опциональный параметр для уведомлений о бронях в группе (см. ниже как получить ID)

3. **Instance Size:**
   - Basic: **$5/месяц** (512 MB RAM, достаточно для бота)
4. Нажмите **Next**

### Шаг 4: Информация и деплой

1. **App Name:** meeting-room-bot
2. **Region:** New York (или тот же что и база данных)
3. Нажмите **Create Resources**

### Шаг 5: Ожидание деплоя

1. DigitalOcean автоматически:
   - ✅ Клонирует код из GitHub
   - ✅ Установит зависимости (requirements.txt)
   - ✅ Запустит бота
2. Следите за процессом в разделе **Build Logs**
3. Деплой займет 2-5 минут

---

## ✅ Проверка работы:

1. В DigitalOcean откройте ваше приложение
2. Перейдите в **Runtime Logs**
3. Вы должны увидеть:

   ```
   База данных инициализирована
   Бот запущен...
   ```

4. Откройте Telegram и напишите боту: `/start`
5. Бот должен ответить! 🎉

---

## � Настройка уведомлений в группе

### Зачем нужно?
Когда пользователь создает бронь, бот автоматически отправляет уведомление в Telegram группу. Таким образом все члены группы видят, кто забронировал комнату и когда.

### Как настроить?

#### 1️⃣ Создайте Telegram группу (или используйте существующую)

#### 2️⃣ Добавьте бота в группу
- Откройте ваш Telegram чат с ботом
- Нажмите на имя бота в заголовке
- **Add Bot to Group** и выберите группу
- **ИЛИ** перейдите в группу и введите: `/start@meeting_room_bot`

#### 3️⃣ Получите ID группы

**Способ 1 - Через @userinfobot (easiest):**
1. Добавьте бота [@userinfobot](https://t.me/userinfobot) в группу
2. Введите `/start` в группе
3. Бот покажет ID группы: `-100123456789`

**Способ 2 - Через forward:**
1. Перейдите в группу
2. Нажмите на любое сообщение → **Copy Link**
3. В ссылке есть ID: `https://t.me/c/123456789/...` → ID это `123456789` (добавьте `-100`)

#### 4️⃣ Установите GROUP_CHAT_ID

1. В DigitalOcean → ваше приложение → **Settings**
2. Найдите компонент `meeting-room-bot`
3. Нажмите **Edit**
4. Прокрутите до **Environment Variables**
5. Нажмите **Edit** и добавьте:
   ```
   GROUP_CHAT_ID = -100123456789
   ```
   (замените на ваш реальный ID)
6. Сохраните - бот перезагрузится

#### 5️⃣ Проверка
- Создайте тестовую бронь через бота
- В группе появится сообщение вроде:
  ```
  📢 НОВАЯ БРОНЬ
  
  👤 Пользователь: Иван Петров
  📅 Дата: 19.01.2026
  ⏰ Время: 14:00 - 15:00
  📝 Описание: Обсуждение проекта
  ```

---

## �🔄 Автоматическое обновление:

**Преимущество GitHub + DigitalOcean:**

- При каждом `git push` в GitHub
- DigitalOcean автоматически перезапустит бота с новым кодом
- Данные в PostgreSQL сохранятся!

**Как обновить бота:**

```powershell
cd C:\Users\Admin\meeting_room_bot
# Внесите изменения в код
git add .
git commit -m "Описание изменений"
git push
# DigitalOcean автоматически обновит бота!
```

---

## 💰 Стоимость:

| Компонент           | План  | Цена        |
| ------------------- | ----- | ----------- |
| PostgreSQL Database | Basic | $15/мес     |
| App (Worker)        | Basic | $5/мес      |
| **ИТОГО**           |       | **$20/мес** |

**Альтернатива дешевле:**

- Railway.app - бесплатно первые $5/мес
- Render.com - бесплатно 750 часов

---

## 📊 Мониторинг:

### Логи бота:

1. В DigitalOcean → Apps → meeting-room-bot
2. **Runtime Logs** - видно все что происходит

### Статус:

- **Running** ✅ - бот работает
- **Deploying** 🔄 - обновление
- **Error** ❌ - проблема (смотрите логи)

### Перезапуск:

1. Apps → meeting-room-bot
2. **...** (три точки) → **Force Rebuild and Deploy**

---

## ❓ Troubleshooting:

### Бот не запускается:

1. Проверьте **Runtime Logs**
2. Убедитесь что `BOT_TOKEN` правильный
3. Проверьте `DATABASE_URL`

### Ошибка подключения к БД:

**Ошибка:** `psycopg2.ProgrammingError: invalid dsn: missing "=" after "${production-database.DATABASE_URL}"`

**Причина:** Переменная окружения `DATABASE_URL` не настроена правильно.

**Решение:**

1. **Вариант 1 - Использование database компонента (рекомендуется):**
   - В вашем приложении в DigitalOcean нажмите **Settings** → **Components**
   - Нажмите **Create** → **Database**
   - Выберите существующую базу данных или создайте новую
   - Имя компонента базы данных (например: `production-database`)
   - В переменных окружения установите: `DATABASE_URL = ${production-database.DATABASE_URL}` (без encrypt!)
   - **ВАЖНО:** `production-database` должно совпадать с именем вашего database компонента

2. **Вариант 2 - Прямая строка подключения:**
   - Откройте вашу базу данных в DigitalOcean
   - Скопируйте **Connection String** (формат: `postgresql://user:password@host:port/db?sslmode=require`)
   - Вставьте полную строку в переменную `DATABASE_URL`
   - Отметьте галочку **Encrypt** для безопасности

3. **После изменения:**
   - Нажмите **Save**
   - DigitalOcean автоматически перезапустит приложение
   - Проверьте логи через 1-2 минуты

### База данных пустая:

- Это нормально при первом запуске
- Таблицы создадутся автоматически
- Первый юзер выберет язык и данные сохранятся

---

## 🎯 Быстрый старт (краткая версия):

```
1. DigitalOcean → Databases → Create PostgreSQL
2. Скопируйте DATABASE_URL
3. Apps → Create → GitHub → meeting-room-bot
4. Add Environment Variables:
   - BOT_TOKEN = ваш_токен
   - DATABASE_URL = скопированная_строка
5. Deploy!
6. Готово! 🚀
```

---

## 📞 Поддержка:

После деплоя бот будет работать 24/7 с сохранением всех данных!

Все брони и языки пользователей будут доступны всем участникам группы.
